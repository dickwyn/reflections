What happens when you initialize a repository? Why do you need to do it?

     when it gets initialized, the folder and all its files gets tracked
	 we need it to log all the changes locally or online

How is the staging area different from the working directory and the repository?
What value do you think it offers?

     staging area is like an airport departure lounge in which the file is just 
	 waiting to be pushed to the cloud. The working directory is different as 
	 it is just the step before staging and the repository is like the destination

How can you use the staging area to make sure you have one commit per logical
change?

     i can check on the difference between the current version before commiting 
	 it to github

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?

     branches will help when trying out a new feature which may affect the program
	 and we'll have like a second version of it which is identical.

How do the diagrams help you visualize the branch structure?

     the diagrams show the flow of the commits and where it breaks into a new 
	 branch

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

     the result is that the commits for both branches are combined and can be 
	 accessed from the same branch

What are the pros and cons of Git’s automatic merging vs. always doing merges
manually?

     merging automatically will save time but manual merges will give the user
	 some form of power over the merged branches
